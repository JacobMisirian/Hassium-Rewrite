func main () {
	MulClass.mul (3, 5);
	MulClass.add (3, 5);
}

class MulClass : AddClass {
	func mul (x, y) {
		println (x * y);
	}
	func div (x, y) {
		println (x / y);
	}
}

class AddClass {
	func add (x, y) {
		println (x + y);
	}
	func sub (x, y) {
		println (x - y);
	}
}
