use Net;

func main () {
	sock = new Socket ("google.com", 80);

	sock.writeLine("GET\n");

	while (1 == 1) {
		line = sock.readLine ();
		if (line.length == -1) {
			break;
		}
		println (line);
	}
}
